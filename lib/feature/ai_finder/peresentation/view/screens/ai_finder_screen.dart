import 'package:flutter/material.dart';

class AIFinderScreen extends StatelessWidget {
  const AIFinderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Ai finder"),
      ),
    );
  }
}